const router = require('express').Router();
const pdfController = require('../controller/controller');

router.post('/id/:id/resume/:name', (req, res) => {

  
});

router.get('/:id', (req, res) => {

})